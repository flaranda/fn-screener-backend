get:
  tags:
    - Criteria
  responses:
    200:
      description: Endpoint to get a list of criteria
      content:
        application/json:
          schema:
            $ref: '../components/schemas/criteria/CriteriaList.yml#/CriteriaList'
