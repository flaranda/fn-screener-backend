get:
  tags:
    - Matching
  responses:
    200:
      description: Endpoint to get a user's selected criteria list
      content:
        application/json:
          schema:
            $ref: '../components/schemas/matching/MatchingList.yml#/MatchingList'
